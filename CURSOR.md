@AGENTS.md

## Cursor-Specific Guidance

- Use Composer (Cmd/Ctrl+I) for multi-file or architectural changes.
- Prefer slash commands from `.cursor/commands/` when available.
- Rules in `.cursor/rules/` provide additional project context.
